# PDF-Relabeler
## Install
1. Use install.sh or MacOS app to create a virtualenv environment and install required python modules
## Usage
1. Add PDFs to be renamed into the *pdfs_to_rename* folder
2. Run the program from a shell or using the run_relabeler.sh/Run Relabeler MacOS app
